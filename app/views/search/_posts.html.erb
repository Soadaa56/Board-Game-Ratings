<div id="posts">
  <% @posts.each do |post| %>
    <tr>
      <td><%= link_to post.title, post %></td>
      <td><%= post.body.truncate_words(25) %></td>
      <td><%= post.ratings.average(:score) %></td>
      <td><%= post.bgg_rating %></td>
    </tr>
  <% end %>
</div>